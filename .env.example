# All optional.
# Absolute site URL used in Open Graph tags. On Vercel it defaults to the production URL; set this for a custom domain.
# VITE_SITE_URL=https://your-domain.example
# Product analytics (PostHog). Without a key, analytics is a no-op (events still log to the console in dev).
# Vercel's native PostHog integration sets NEXT_PUBLIC_POSTHOG_KEY / NEXT_PUBLIC_POSTHOG_HOST; those are picked up automatically.
# VITE_POSTHOG_KEY=phc_xxx
# VITE_POSTHOG_HOST=https://us.i.posthog.com
# Deezer proxy (api/deezer.js): restrict CORS to other origins that may call it. Same-origin needs nothing.
# ALLOWED_ORIGIN=https://your-domain.example
