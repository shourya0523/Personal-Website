# No environment variables are required for local development.
# The Music app's search uses the Vercel edge function in api/deezer.js; run `vercel dev` to exercise it locally.
# ALLOWED_ORIGIN=https://your-domain.example  (optional, restricts the deezer proxy's CORS origin)
