# One Wish Willow

Place the forthcoming 3D asset here, e.g.:

- `one-wish-willow.glb` (preferred), or
- OBJ/MTL folder matching `public/models/indoor-plant/`

`WillowScene` will load `one-wish-willow.glb` when present.
